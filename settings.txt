You will need to fill out this file for hand in. 
The bellow is just an example, feel free to disregard this documents contents.


Your final settings.txt will look something like this when you are finished:

/////////////////////////////////

Wallpaper 1:

 let ballX = 90 // controls the black ball itself
 let ballY = 125 
 let hatX = 75 // controls the purple hat he wears
 let hatY = 95
 let sparkleX = 150 // controls the sparkles on the top right
 let sparkleY = 50
 let moustacheX = 80 // controls the moustache
 let moustacheY = 125

 let drawsymbol = true // TRUE draws the symbol.FALSE turns this 'if statement' off
 let nofacialhair = false // TRUE removes the moustache. FALSE turns this 'if statement' off
 let casualeightball = false // TRUE removes the moustache, hat and sparkles. FALSE turns this 'if statement' off
 let casualdouble = false // TRUE makes two of them. FALSE turns this 'if statement' off
 let miniballs = false // TRUE makes multiple mini symbols. FALSE turns this 'if statement' off

function setup_wallpaper(pWallpaper) {
  pWallpaper.output_mode(GRID_WALLPAPER);
  pWallpaper.resolution(NINE_PORTRAIT);
  pWallpaper.show_guide(false); //set this to false when you're ready to print (to remove the lines)

  //Grid settings
  pWallpaper.grid_settings.cell_width  = 200;
  pWallpaper.grid_settings.cell_height = 200;
  pWallpaper.grid_settings.row_offset  = 50;
}

function wallpaper_background() {
 background(62, 60, 125); //midnight sky blue
}

/////////////////////////////////

Wallpaper 2:

let ballX = 90 // controls the black ball itself
 let ballY = 125 
 let hatX = 75 // controls the purple hat he wears
 let hatY = 95
 let sparkleX = 150 // controls the sparkles on the top right
 let sparkleY = 50
 let moustacheX = 80 // controls the moustache
 let moustacheY = 125

 let drawsymbol = false // TRUE draws the symbol.FALSE turns this 'if statement' off
 let nofacialhair = true// TRUE removes the moustache. FALSE turns this 'if statement' off
 let casualeightball = false // TRUE removes the moustache, hat and sparkles. FALSE turns this 'if statement' off
 let casualdouble = false // TRUE makes two of them. FALSE turns this 'if statement' off
 let miniballs = false // TRUE makes multiple mini symbols. FALSE turns this 'if statement' off

function setup_wallpaper(pWallpaper) {
  pWallpaper.output_mode(GLIDE_WALLPAPER);
  pWallpaper.resolution(NINE_PORTRAIT);
  pWallpaper.show_guide(false); //set this to false when you're ready to print (to remove the lines)

  //Grid settings
  pWallpaper.grid_settings.cell_width  = 200;
  pWallpaper.grid_settings.cell_height = 200;
  pWallpaper.grid_settings.row_offset  = 25;
}

function wallpaper_background() {
 background(62, 60, 125); //midnight sky blue
}

/////////////////////////////////

Wallpaper 3:

let ballX = 90 // controls the black ball itself
 let ballY = 125
 let hatX = 75 // controls the purple hat he wears
 let hatY = 95
 let sparkleX = 150 // controls the sparkles on the top right
 let sparkleY = 50
 let moustacheX = 80 // controls the moustache
 let moustacheY = 125

 let drawsymbol = false // TRUE draws the symbol.FALSE turns this 'if statement' off
 let nofacialhair = false// TRUE removes the moustache. FALSE turns this 'if statement' off
 let casualeightball = true // TRUE removes the moustache, hat and sparkles. FALSE turns this 'if statement' off
 let casualdouble = false // TRUE makes two of them. FALSE turns this 'if statement' off
 let miniballs = false // TRUE makes multiple mini symbols. FALSE turns this 'if statement' off

function setup_wallpaper(pWallpaper) {
  pWallpaper.output_mode(GRID_WALLPAPER);
  pWallpaper.resolution(NINE_PORTRAIT);
  pWallpaper.show_guide(false); //set this to false when you're ready to print (to remove the lines)

  //Grid settings
  pWallpaper.grid_settings.cell_width  = 200;
  pWallpaper.grid_settings.cell_height = 200;
  pWallpaper.grid_settings.row_offset  = 50;
}

function wallpaper_background() {
 background(55, 59, 56); //grey colour
}

/////////////////////////////////

Wallpaper 4:

let ballX = 90 // controls the black ball itself
 let ballY = 125 
 let hatX = 75 // controls the purple hat he wears
 let hatY = 95
 let sparkleX = 150 // controls the sparkles on the top right
 let sparkleY = 50
 let moustacheX = 80 // controls the moustache
 let moustacheY = 125

 let drawsymbol = false // TRUE draws the symbol.FALSE turns this 'if statement' off
 let nofacialhair = false// TRUE removes the moustache. FALSE turns this 'if statement' off
 let casualeightball = true // TRUE removes the moustache, hat and sparkles. FALSE turns this 'if statement' off
 let casualdouble = false // TRUE makes two of them. FALSE turns this 'if statement' off
 let miniballs = false // TRUE makes multiple mini symbols. FALSE turns this 'if statement' off

function setup_wallpaper(pWallpaper) {
  //pWallpaper.output_mode(DEVELOP_GLYPH);
  pWallpaper.output_mode(GRID_WALLPAPER);
  //pWallpaper.output_mode(GLIDE_WALLPAPER);
  pWallpaper.resolution(NINE_PORTRAIT);
  pWallpaper.show_guide(false); //set this to false when you're ready to print (to remove the lines)

  //Grid settings
  pWallpaper.grid_settings.cell_width  = 100;
  pWallpaper.grid_settings.cell_height = 75;
  pWallpaper.grid_settings.row_offset  = 50;
}

function wallpaper_background() {
 background(62, 60, 125); //midnight sky blue
}

/////////////////////////////////

Wallpaper 5:

let ballX = 90 // controls the black ball itself
 let ballY = 125 
 let hatX = 75 // controls the purple hat he wears
 let hatY = 95
 let sparkleX = 150 // controls the sparkles on the top right
 let sparkleY = 50
 let moustacheX = 80 // controls the moustache
 let moustacheY = 125

 let drawsymbol = false // TRUE draws the symbol.FALSE turns this 'if statement' off
 let nofacialhair = false// TRUE removes the moustache. FALSE turns this 'if statement' off
 let casualeightball = false // TRUE removes the moustache, hat and sparkles. FALSE turns this 'if statement' off
 let casualdouble = true // TRUE makes two of them. FALSE turns this 'if statement' off
 let miniballs = false // TRUE makes multiple mini symbols. FALSE turns this 'if statement' off

function setup_wallpaper(pWallpaper) {
  pWallpaper.output_mode(GRID_WALLPAPER);
  pWallpaper.resolution(NINE_PORTRAIT);
  pWallpaper.show_guide(false); //set this to false when you're ready to print (to remove the lines)

  //Grid settings
  pWallpaper.grid_settings.cell_width  = 200;
  pWallpaper.grid_settings.cell_height = 200;
  pWallpaper.grid_settings.row_offset  = 50;
}

function wallpaper_background() {
 background(65, 173, 38); //pool table colour
}

/////////////////////////////////

Wallpaper 6:

 let ballX = 90 // controls the black ball itself
 let ballY = 125 
 let hatX = 75 // controls the purple hat he wears
 let hatY = 95
 let sparkleX = 150 // controls the sparkles on the top right
 let sparkleY = 50
 let moustacheX = 80 // controls the moustache
 let moustacheY = 125

 let drawsymbol = false // TRUE draws the symbol.FALSE turns this 'if statement' off
 let nofacialhair = false// TRUE removes the moustache. FALSE turns this 'if statement' off
 let casualeightball = false // TRUE removes the moustache, hat and sparkles. FALSE turns this 'if statement' off
 let casualdouble = false // TRUE makes two of them. FALSE turns this 'if statement' off
 let miniballs = true // TRUE makes multiple mini symbols. FALSE turns this 'if statement' off

function setup_wallpaper(pWallpaper) {
  //pWallpaper.output_mode(DEVELOP_GLYPH);
  pWallpaper.output_mode(GRID_WALLPAPER);
  //pWallpaper.output_mode(GLIDE_WALLPAPER);
  pWallpaper.resolution(NINE_PORTRAIT);
  pWallpaper.show_guide(false); //set this to false when you're ready to print (to remove the lines)

  //Grid settings
  pWallpaper.grid_settings.cell_width  = 200;
  pWallpaper.grid_settings.cell_height = 200;
  pWallpaper.grid_settings.row_offset  = 50;
}

function wallpaper_background() {
 background(62, 60, 125); //midnight sky blue
}

/////////////////////////////////

Wallpaper 7:

let ballX = 90 // controls the black ball itself
 let ballY = 125 
 let hatX = 75 // controls the purple hat he wears
 let hatY = 95
 let sparkleX = 150 // controls the sparkles on the top right
 let sparkleY = 50
 let moustacheX = 80 // controls the moustache
 let moustacheY = 125

 let drawsymbol = false // TRUE draws the symbol.FALSE turns this 'if statement' off
 let nofacialhair = false// TRUE removes the moustache. FALSE turns this 'if statement' off
 let casualeightball = false // TRUE removes the moustache, hat and sparkles. FALSE turns this 'if statement' off
 let casualdouble = false // TRUE makes two of them. FALSE turns this 'if statement' off
 let miniballs = true // TRUE makes multiple mini symbols. FALSE turns this 'if statement' off

function setup_wallpaper(pWallpaper) {
  pWallpaper.output_mode(GRID_WALLPAPER);
  pWallpaper.resolution(NINE_PORTRAIT);
  pWallpaper.show_guide(false); //set this to false when you're ready to print (to remove the lines)

  //Grid settings
  pWallpaper.grid_settings.cell_width  = 195;
  pWallpaper.grid_settings.cell_height = 150;
  pWallpaper.grid_settings.row_offset  = 100;
}

function wallpaper_background() {
 background(55, 59, 56); //grey colour
}

/////////////////////////////////

Wallpaper 8:

let ballX = 133 // controls the black ball itself
 let ballY = 150 
 let hatX = 50 // controls the purple hat he wears
 let hatY = 80
 let sparkleX = 100 // controls the sparkles on the top right
 let sparkleY = 25
 let moustacheX = 30 // controls the moustache
 let moustacheY = 95

 let drawsymbol = true // TRUE draws the symbol.FALSE turns this 'if statement' off
 let nofacialhair = false// TRUE removes the moustache. FALSE turns this 'if statement' off
 let casualeightball = false // TRUE removes the moustache, hat and sparkles. FALSE turns this 'if statement' off
 let casualdouble = false // TRUE makes two of them. FALSE turns this 'if statement' off
 let miniballs = false // TRUE makes multiple mini symbols. FALSE turns this 'if statement' off

function setup_wallpaper(pWallpaper) {
  pWallpaper.output_mode(GLIDE_WALLPAPER);
  pWallpaper.resolution(NINE_PORTRAIT);
  pWallpaper.show_guide(false); //set this to false when you're ready to print (to remove the lines)

  //Grid settings
  pWallpaper.grid_settings.cell_width  = 220;
  pWallpaper.grid_settings.cell_height = 200;
  pWallpaper.grid_settings.row_offset  = 130;
}

function wallpaper_background() {
 background(62, 60, 125); //midnight sky blue
}

/////////////////////////////////

Wallpaper 9:

let ballX = 157 // controls the black ball itself
 let ballY = 120 
 let hatX = 43 // controls the purple hat he wears
 let hatY = 115
 let sparkleX = 93 // controls the sparkles on the top right
 let sparkleY = 70
 let moustacheX = 23 // controls the moustache
 let moustacheY = 130

 let drawsymbol = true // TRUE draws the symbol.FALSE turns this 'if statement' off
 let nofacialhair = false// TRUE removes the moustache. FALSE turns this 'if statement' off
 let casualeightball = false // TRUE removes the moustache, hat and sparkles. FALSE turns this 'if statement' off
 let casualdouble = false // TRUE makes two of them. FALSE turns this 'if statement' off
 let miniballs = false // TRUE makes multiple mini symbols. FALSE turns this 'if statement' off

function setup_wallpaper(pWallpaper) {
  pWallpaper.output_mode(GLIDE_WALLPAPER);
  pWallpaper.resolution(NINE_PORTRAIT);
  pWallpaper.show_guide(false); //set this to false when you're ready to print (to remove the lines)

  //Grid settings
  pWallpaper.grid_settings.cell_width  = 110;
  pWallpaper.grid_settings.cell_height = 250;
  pWallpaper.grid_settings.row_offset  = 130;
}

function wallpaper_background() {
 background(62, 60, 125); //midnight sky blue
}

/////////////////////////////////